---
phase: 02-landing-page
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/pages/index.astro
  - tests/landing-hero.spec.ts
  - tests/landing-sections.spec.ts
  - tests/landing-features.spec.ts
  - tests/landing-animations.spec.ts
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05
  - LAND-06
  - LAND-07
  - LAND-08
  - LAND-09

must_haves:
  truths:
    - "Homepage loads with full landing page content (not stub placeholder)"
    - "Scrolling down the page reveals sections with fade+slide-up animations"
    - "All anchor links work: Nav Features -> #features, Hero See How It Works -> #how-it-works"
    - "All CTA buttons link to app.getfierro.com/signup"
    - "Page visual rhythm follows dark-light-light-dark-dark pattern"
    - "Playwright tests pass for all LAND-* requirements"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete landing page composing all section components with IntersectionObserver script"
      min_lines: 30
      contains: "IntersectionObserver"
    - path: "tests/landing-hero.spec.ts"
      provides: "Tests for LAND-01 (hero section) and LAND-02 (CTAs)"
    - path: "tests/landing-sections.spec.ts"
      provides: "Tests for LAND-03 (skipped trust bar), LAND-04 (value props), LAND-06 (how it works), LAND-07 (skipped personas), LAND-08 (closing CTA)"
    - path: "tests/landing-features.spec.ts"
      provides: "Tests for LAND-05 (feature showcase with 5 features)"
    - path: "tests/landing-animations.spec.ts"
      provides: "Tests for LAND-09 (scroll animation classes)"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/landing/Hero.astro"
      via: "Astro component import"
      pattern: "import Hero"
    - from: "src/pages/index.astro"
      to: "src/components/landing/ValueProps.astro"
      via: "Astro component import"
      pattern: "import ValueProps"
    - from: "src/pages/index.astro"
      to: "src/components/landing/FeatureShowcase.astro"
      via: "Astro component import"
      pattern: "import FeatureShowcase"
    - from: "src/pages/index.astro"
      to: "src/components/landing/HowItWorks.astro"
      via: "Astro component import"
      pattern: "import HowItWorks"
    - from: "src/pages/index.astro"
      to: "src/components/landing/ClosingCta.astro"
      via: "Astro component import"
      pattern: "import ClosingCta"
    - from: "src/pages/index.astro"
      to: "[data-animate] elements"
      via: "IntersectionObserver script adds .visible class"
      pattern: "IntersectionObserver"
---

<objective>
Assemble the complete landing page from all section components, add the IntersectionObserver scroll animation script, and write the Playwright test suite covering all LAND-* requirements.

Purpose: This is the wiring plan -- it takes all components built by Plan 01 and Plan 02, composes them into the final index.astro page, and validates everything works with automated tests. This plan covers ALL LAND-* requirement IDs because its tests verify the integrated page.

Output: Complete index.astro replacing stub, 4 Playwright test files, full test suite passing
</objective>

<execution_context>
@/Users/jesus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jesus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page/02-CONTEXT.md
@.planning/phases/02-landing-page/02-RESEARCH.md
@.planning/phases/02-landing-page/02-01-SUMMARY.md
@.planning/phases/02-landing-page/02-02-SUMMARY.md

<interfaces>
<!-- Components created by Plan 01 -->
From src/components/landing/Hero.astro:
- Self-contained section component, no props
- Contains data-animate attributes on text and illustration columns

From src/components/landing/ValueProps.astro:
- Self-contained section component, no props
- Contains data-animate-stagger on parent, data-animate on each card

<!-- Components created by Plan 02 -->
From src/components/landing/FeatureShowcase.astro:
- Self-contained section component, no props
- Has id="features" anchor
- Contains 5 FeatureRow instances with data-animate

From src/components/landing/HowItWorks.astro:
- Self-contained section component, no props
- Has id="how-it-works" anchor
- Contains data-animate-stagger and data-animate

From src/components/landing/ClosingCta.astro:
- Self-contained section component, no props
- Contains data-animate

<!-- Existing infrastructure -->
From src/layouts/BaseLayout.astro:
```astro
interface Props { title: string; }
```
Wraps in html > body > Nav + MobileMenu + main > slot + Footer

From src/styles/global.css (extended by Plan 01):
```css
[data-animate] { opacity: 0; transform: translateY(20px); transition: ... }
[data-animate].visible { opacity: 1; transform: translateY(0); }
```

From playwright.config.ts:
- Chromium-only project
- webServer starts Astro on port 4321
- Test directory: tests/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble index.astro with all section components and IntersectionObserver script</name>
  <files>src/pages/index.astro</files>
  <action>
Replace the existing stub index.astro with the complete landing page composition.

1. Frontmatter imports:
   ```astro
   ---
   import BaseLayout from '../layouts/BaseLayout.astro';
   import Hero from '../components/landing/Hero.astro';
   import ValueProps from '../components/landing/ValueProps.astro';
   import FeatureShowcase from '../components/landing/FeatureShowcase.astro';
   import HowItWorks from '../components/landing/HowItWorks.astro';
   import ClosingCta from '../components/landing/ClosingCta.astro';
   ---
   ```

2. Template composition (order matters for page flow):
   ```astro
   <BaseLayout title="Construction Cost Control">
     <Hero />
     <ValueProps />
     <FeatureShowcase />
     <HowItWorks />
     <ClosingCta />
   </BaseLayout>
   ```

3. IntersectionObserver script (placed after the BaseLayout closing tag):
   ```astro
   <script>
     document.addEventListener('astro:page-load', () => {
       const targets = document.querySelectorAll('[data-animate]');
       if (!targets.length) return;

       const observer = new IntersectionObserver(
         (entries) => {
           entries.forEach((entry) => {
             if (entry.isIntersecting) {
               entry.target.classList.add('visible');
               observer.unobserve(entry.target);
             }
           });
         },
         { threshold: 0.15, rootMargin: '0px 0px -50px 0px' }
       );

       targets.forEach((el) => observer.observe(el));
     });
   </script>
   ```

Key details:
- Use `astro:page-load` event (not DOMContentLoaded) for ClientRouter compatibility
- threshold 0.15 means element must be 15% visible before triggering
- rootMargin -50px on bottom means element must be 50px into viewport
- observer.unobserve ensures one-time reveal (animation doesn't replay on scroll back)
- Title "Construction Cost Control" for the page title (renders as "Construction Cost Control | Fierro" via BaseLayout)

4. Add a noscript style block for users with JS disabled:
   ```astro
   <noscript>
     <style>
       [data-animate] { opacity: 1 !important; transform: none !important; }
     </style>
   </noscript>
   ```
   Place this inside the BaseLayout slot, before the Hero component.
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && grep -c "import Hero" src/pages/index.astro && grep -c "import ClosingCta" src/pages/index.astro && grep -c "IntersectionObserver" src/pages/index.astro && grep -c "noscript" src/pages/index.astro && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>index.astro composes all 5 section components in correct order (Hero, ValueProps, FeatureShowcase, HowItWorks, ClosingCta) within BaseLayout. IntersectionObserver script toggles .visible class on [data-animate] elements. Noscript fallback ensures content visible without JS. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Write Playwright test suite for all LAND-* requirements</name>
  <files>tests/landing-hero.spec.ts, tests/landing-sections.spec.ts, tests/landing-features.spec.ts, tests/landing-animations.spec.ts</files>
  <action>
Create 4 Playwright test files following the existing test pattern from Phase 1 (see tests/nav.spec.ts for conventions).

**tests/landing-hero.spec.ts** (covers LAND-01, LAND-02):
```typescript
import { test, expect } from '@playwright/test';

test.describe('Landing Page - Hero Section', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });

  // LAND-01: Dark premium hero section
  test('hero section has dark gunmetal background', async ({ page }) => {
    const hero = page.locator('section').first();
    await expect(hero).toBeVisible();
    // Check for bg-gunmetal class (Tailwind applies the color)
    await expect(hero).toHaveCSS('background-color', /./);  // non-empty
  });

  test('hero displays headline text', async ({ page }) => {
    await expect(page.locator('h1')).toContainText('Every dollar. Every pour. Accounted for.');
  });

  test('hero contains SVG illustration', async ({ page }) => {
    // Hero section should contain an inline SVG element
    const heroSvg = page.locator('section').first().locator('svg');
    await expect(heroSvg.first()).toBeVisible();
  });

  // LAND-02: Two CTAs above the fold
  test('Start Free CTA links to signup', async ({ page }) => {
    const startFree = page.locator('section').first().locator('a', { hasText: 'Start Free' });
    await expect(startFree).toBeVisible();
    await expect(startFree).toHaveAttribute('href', 'https://app.getfierro.com/signup');
  });

  test('See How It Works CTA links to anchor', async ({ page }) => {
    const howItWorks = page.locator('a', { hasText: 'See How It Works' });
    await expect(howItWorks).toBeVisible();
    await expect(howItWorks).toHaveAttribute('href', '#how-it-works');
  });
});
```

**tests/landing-sections.spec.ts** (covers LAND-03 skipped, LAND-04, LAND-06, LAND-07 skipped, LAND-08):
```typescript
import { test, expect } from '@playwright/test';

test.describe('Landing Page - Sections', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });

  // LAND-03: Trust bar SKIPPED -- verify it's not present
  test('no trust/logo bar section exists (deferred)', async ({ page }) => {
    // Verify no element with typical trust bar indicators
    const trustBar = page.locator('[data-testid="trust-bar"], .trust-bar, .logo-bar');
    await expect(trustBar).toHaveCount(0);
  });

  // LAND-04: Value proposition cards
  test('value proposition cards are visible', async ({ page }) => {
    // Should have 3-4 cards
    const cards = page.locator('[data-animate]').filter({ has: page.locator('h3') });
    const count = await cards.count();
    expect(count).toBeGreaterThanOrEqual(3);
  });

  // LAND-06: How It Works section
  test('How It Works section has 3 steps', async ({ page }) => {
    const section = page.locator('#how-it-works');
    await expect(section).toBeVisible();
    // Should have 3 step items (look for heading elements within)
    const steps = section.locator('h3');
    await expect(steps).toHaveCount(3);
  });

  // LAND-07: Persona callouts SKIPPED -- verify not present
  test('no persona callout section exists (deferred)', async ({ page }) => {
    const personas = page.locator('[data-testid="personas"], .persona-callouts');
    await expect(personas).toHaveCount(0);
    // Also verify no "For General Contractors" / "For Subcontractors" / "For Project Owners" text
    await expect(page.locator('text=For General Contractors')).toHaveCount(0);
    await expect(page.locator('text=For Subcontractors')).toHaveCount(0);
    await expect(page.locator('text=For Project Owners')).toHaveCount(0);
  });

  // LAND-08: Closing CTA
  test('closing CTA has Start Free button linking to signup', async ({ page }) => {
    // Last section before footer should have the CTA
    const closingCta = page.locator('a[href="https://app.getfierro.com/signup"]').last();
    await expect(closingCta).toBeVisible();
    await expect(closingCta).toContainText('Start Free');
  });
});
```

**tests/landing-features.spec.ts** (covers LAND-05):
```typescript
import { test, expect } from '@playwright/test';

test.describe('Landing Page - Feature Showcase', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });

  // LAND-05: Feature showcase with 5 features
  test('features section exists with correct anchor', async ({ page }) => {
    const section = page.locator('#features');
    await expect(section).toBeVisible();
  });

  test('features section displays 5 feature headings', async ({ page }) => {
    const section = page.locator('#features');
    // Each feature row has an h3
    const featureHeadings = section.locator('h3');
    await expect(featureHeadings).toHaveCount(5);
  });

  test('feature headings include expected capabilities', async ({ page }) => {
    const section = page.locator('#features');
    // Verify the 5 required features are present (checking for keywords)
    await expect(section.locator('h3', { hasText: /budget/i })).toBeVisible();
    await expect(section.locator('h3', { hasText: /expense/i })).toBeVisible();
    await expect(section.locator('h3', { hasText: /team/i })).toBeVisible();
    await expect(section.locator('h3', { hasText: /vendor/i })).toBeVisible();
    await expect(section.locator('h3', { hasText: /analytics|reporting/i })).toBeVisible();
  });

  test('each feature has an SVG illustration', async ({ page }) => {
    const section = page.locator('#features');
    const svgs = section.locator('svg');
    const count = await svgs.count();
    expect(count).toBeGreaterThanOrEqual(5);
  });

  test('nav Features link scrolls to features section', async ({ page }) => {
    // Click Features in nav (desktop)
    await page.setViewportSize({ width: 1280, height: 720 });
    await page.click('a[href="/#features"]');
    // Verify features section is in viewport
    const features = page.locator('#features');
    await expect(features).toBeInViewport({ ratio: 0.1 });
  });
});
```

**tests/landing-animations.spec.ts** (covers LAND-09):
```typescript
import { test, expect } from '@playwright/test';

test.describe('Landing Page - Scroll Animations', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });

  // LAND-09: Scroll-triggered CSS animations
  test('data-animate elements exist on the page', async ({ page }) => {
    const animated = page.locator('[data-animate]');
    const count = await animated.count();
    expect(count).toBeGreaterThan(0);
  });

  test('elements near top of page receive visible class on load', async ({ page }) => {
    // Hero elements should be immediately visible (already in viewport)
    await page.waitForTimeout(1000); // Allow observer to fire
    const heroAnimated = page.locator('section').first().locator('[data-animate].visible');
    const count = await heroAnimated.count();
    expect(count).toBeGreaterThan(0);
  });

  test('elements below fold do not have visible class initially', async ({ page }) => {
    // Closing CTA is at bottom -- should NOT be visible yet
    // This tests that animation is scroll-triggered, not instant
    const closingSection = page.locator('section').last();
    const animated = closingSection.locator('[data-animate]:not(.visible)');
    // At least one element below fold should still be hidden
    // (depends on viewport -- may need to scroll to verify)
    const count = await animated.count();
    // This may be 0 if page is short enough -- test is advisory
  });

  test('scrolling reveals elements with visible class', async ({ page }) => {
    // Scroll to bottom of page
    await page.evaluate(() => window.scrollTo(0, document.body.scrollHeight));
    await page.waitForTimeout(1000); // Allow observer + transitions
    // After scrolling to bottom, all data-animate elements should have .visible
    const allAnimated = page.locator('[data-animate]');
    const allVisible = page.locator('[data-animate].visible');
    const totalCount = await allAnimated.count();
    const visibleCount = await allVisible.count();
    expect(visibleCount).toBe(totalCount);
  });

  test('prefers-reduced-motion is respected in CSS', async ({ page }) => {
    // Verify the CSS contains the reduced-motion media query
    const styles = await page.evaluate(() => {
      const sheets = Array.from(document.styleSheets);
      for (const sheet of sheets) {
        try {
          const rules = Array.from(sheet.cssRules);
          for (const rule of rules) {
            if (rule instanceof CSSMediaRule && rule.conditionText?.includes('prefers-reduced-motion')) {
              return true;
            }
          }
        } catch { /* cross-origin sheets */ }
      }
      return false;
    });
    expect(styles).toBe(true);
  });
});
```

IMPORTANT: Adapt the exact selectors if the component structure from Plans 01/02 differs slightly from what's specified here. Read the actual component files first (Hero.astro, ValueProps.astro, FeatureShowcase.astro, HowItWorks.astro, ClosingCta.astro) before writing the tests to match the real DOM structure.

Follow the existing test conventions from tests/nav.spec.ts (import pattern, describe blocks, beforeEach with page.goto).
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && npx playwright test --project=chromium -x 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 Playwright test files exist and pass. Tests cover: LAND-01 (hero bg + headline + SVG), LAND-02 (two CTAs with correct hrefs), LAND-03 (trust bar absent), LAND-04 (value props visible), LAND-05 (5 features with anchors + SVGs), LAND-06 (3 How It Works steps), LAND-07 (persona callouts absent), LAND-08 (closing CTA with signup link), LAND-09 (scroll animations with visible class toggling). Full test suite passes.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds
- `npx playwright test --project=chromium` -- all tests pass
- Homepage at localhost:4321 shows full landing page (not stub)
- Scrolling triggers fade+slide-up animations on sections
- Nav "Features" link scrolls to #features section
- Hero "See How It Works" scrolls to #how-it-works section
- All "Start Free" buttons link to app.getfierro.com/signup
- Content is visible with JavaScript disabled (noscript fallback)
</verification>

<success_criteria>
- index.astro composes Hero, ValueProps, FeatureShowcase, HowItWorks, ClosingCta in correct order
- IntersectionObserver script runs on astro:page-load and adds .visible class to [data-animate] elements
- Noscript fallback ensures content visible without JS
- All 4 Playwright test files pass covering every LAND-* requirement
- Page visual rhythm: dark (hero) -> light (value props) -> light (features) -> dark (how it works) -> dark (closing CTA)
- No new npm dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-03-SUMMARY.md`
</output>
