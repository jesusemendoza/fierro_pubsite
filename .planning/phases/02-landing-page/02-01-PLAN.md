---
phase: 02-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/landing/Hero.astro
  - src/components/landing/ValueProps.astro
  - src/assets/illustrations/hero-dashboard.svg
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-09

must_haves:
  truths:
    - "Hero section has solid dark Gunmetal background with headline 'Every dollar. Every pour. Accounted for.'"
    - "Two CTAs visible: filled Molten Orange 'Start Free' linking to app.getfierro.com/signup and ghost outline 'See How It Works' linking to #how-it-works anchor"
    - "3-4 value proposition cards display quantified benefit claims on off-white background"
    - "CSS animation styles for data-animate fade+slide-up pattern exist in global.css"
    - "Trust bar is intentionally omitted (LAND-03 satisfied by user decision to skip)"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Scroll animation CSS, smooth scroll, scroll-margin-top, reduced-motion support"
      contains: "[data-animate]"
    - path: "src/components/landing/Hero.astro"
      provides: "Dark hero section with headline, subtitle, two CTAs, SVG illustration"
      min_lines: 30
    - path: "src/components/landing/ValueProps.astro"
      provides: "3-4 value proposition cards in grid layout"
      min_lines: 20
    - path: "src/assets/illustrations/hero-dashboard.svg"
      provides: "Abstract SVG illustration for hero section"
  key_links:
    - from: "src/components/landing/Hero.astro"
      to: "https://app.getfierro.com/signup"
      via: "anchor href"
      pattern: "app\\.getfierro\\.com/signup"
    - from: "src/components/landing/Hero.astro"
      to: "#how-it-works"
      via: "anchor href"
      pattern: "#how-it-works"
---

<objective>
Build the animation infrastructure, hero section, and value proposition cards for the Fierro landing page.

Purpose: Establishes the scroll animation CSS system used by all sections and implements the top two content sections (hero + value props) that form the above-the-fold experience. LAND-03 (trust bar) and LAND-07 (persona callouts) are explicitly skipped per user decision.

Output: global.css extended with animation styles, Hero.astro component, ValueProps.astro component, hero SVG illustration
</objective>

<execution_context>
@/Users/jesus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jesus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page/02-CONTEXT.md
@.planning/phases/02-landing-page/02-RESEARCH.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
}
```
BaseLayout wraps content in <main><slot /></main> with Nav, MobileMenu, Footer.

From src/styles/global.css:
```css
@theme {
  --color-gunmetal: oklch(0.235 0.006 265);
  --color-molten-orange: oklch(0.62 0.19 48);
  --color-off-white: oklch(0.97 0.005 90);
  --color-concrete-gray: oklch(0.63 0.006 265);
  --color-rebar-green: oklch(0.53 0.12 160);
  --color-overrun-red: oklch(0.55 0.22 25);
  --font-sans: 'DM Sans Variable', system-ui, sans-serif;
}
```

From src/components/Nav.astro:
- Nav is `fixed top-0 z-50 bg-transparent` with scroll-based transition to bg-gunmetal
- Nav height is approximately 64px (py-4 + logo h-8)
- "Features" link already points to `/#features`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend global.css with scroll animation infrastructure</name>
  <files>src/styles/global.css</files>
  <action>
Extend the existing global.css (which already has @import "tailwindcss", @import "@fontsource-variable/dm-sans", and @theme block) with the following additions AFTER the existing @theme block:

1. Add `html { scroll-behavior: smooth; }` for anchor link smooth scrolling

2. Add `[data-animate]` base styles:
   - opacity: 0; transform: translateY(20px)
   - transition: opacity 600ms ease-out, transform 600ms ease-out

3. Add `[data-animate].visible` styles:
   - opacity: 1; transform: translateY(0)

4. Add stagger delay rules using `[data-animate-stagger] > [data-animate]:nth-child(N)` pattern with 100ms increments for children 1-4

5. Add `[id] { scroll-margin-top: 5rem; }` to offset anchors below the fixed nav

6. Add `@media (prefers-reduced-motion: reduce)` block that:
   - Sets html scroll-behavior to auto
   - Sets [data-animate] to opacity: 1, transform: none, transition: none

7. Add a `noscript` fallback comment explaining that the IntersectionObserver script in index.astro handles the noscript case by adding visible class on load

DO NOT modify the existing @import statements or @theme block. Only append new rules after the existing content.

Reference the complete CSS from RESEARCH.md Pattern 5 (CSS Transition Approach) for exact syntax.
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && grep -c "data-animate" src/styles/global.css && grep -c "prefers-reduced-motion" src/styles/global.css && grep -c "scroll-behavior" src/styles/global.css && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>global.css contains [data-animate] transition styles, stagger delays, scroll-margin-top offset, smooth scroll, and reduced-motion media query. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero section component with SVG illustration</name>
  <files>src/components/landing/Hero.astro, src/assets/illustrations/hero-dashboard.svg</files>
  <action>
Create two files:

**src/assets/illustrations/hero-dashboard.svg:**
Create an abstract SVG illustration (under 5KB) suggesting a dashboard/construction context. Use geometric shapes: rounded rectangles for screen/cards, circles and lines for data visualizations, small bar chart shapes. Use brand colors as fill values -- use the actual OKLch values or hex equivalents since SVGs don't read CSS custom properties: Molten Orange for accent elements, Off-White/light gray for primary shapes, Concrete Gray for secondary shapes. Include a viewBox attribute for responsiveness. The illustration should evoke "financial dashboard" without being a literal screenshot.

**src/components/landing/Hero.astro:**
Create the hero section component with:

1. Import the hero SVG: `import HeroDashboard from '../../assets/illustrations/hero-dashboard.svg';`

2. Outer `<section>` with:
   - `class="relative bg-gunmetal min-h-[80vh] flex items-center pt-20 pb-16 lg:pb-24"`
   - pt-20 accounts for the fixed nav height

3. Inner container `div.mx-auto.max-w-7xl.px-4.sm:px-6.lg:px-8`

4. Flex layout: `flex flex-col items-center gap-12 lg:flex-row lg:items-center lg:gap-16`

5. Text column (left side on desktop, top on mobile) with `data-animate`:
   - h1: "Every dollar. Every pour. Accounted for." -- text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight text-off-white
   - p subtitle: "Construction cost control that keeps your projects profitable." -- mt-6 text-lg text-concrete-gray (verify contrast -- use text-off-white/70 if concrete-gray is too dim on gunmetal)
   - CTA container: mt-8 flex flex-col gap-4 sm:flex-row sm:justify-center lg:justify-start
   - Primary CTA: `<a href="https://app.getfierro.com/signup">` with rounded-md bg-molten-orange px-6 py-3 text-center font-semibold text-white hover:bg-molten-orange/90 transition-colors. Text: "Start Free"
   - Secondary CTA: `<a href="#how-it-works">` with rounded-md border border-off-white/30 px-6 py-3 text-center font-semibold text-off-white hover:border-off-white/60 hover:bg-off-white/10 transition-colors. Text: "See How It Works"

6. Illustration column (right side on desktop, bottom on mobile) with `data-animate`:
   - `<HeroDashboard class="w-full h-auto max-w-lg" />` -- renders inline SVG

No `<script>` tag in this component -- the IntersectionObserver lives in index.astro (Plan 03).
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && test -f src/components/landing/Hero.astro && test -f src/assets/illustrations/hero-dashboard.svg && grep -c "app.getfierro.com/signup" src/components/landing/Hero.astro && grep -c "#how-it-works" src/components/landing/Hero.astro && grep -c "data-animate" src/components/landing/Hero.astro && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>Hero.astro renders a dark Gunmetal section with the headline "Every dollar. Every pour. Accounted for.", subtitle, two CTAs (Start Free linking to signup, See How It Works linking to #how-it-works), and an abstract SVG illustration. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Create Value Proposition cards component</name>
  <files>src/components/landing/ValueProps.astro</files>
  <action>
Create the value proposition section component:

1. Outer `<section>` with:
   - `class="bg-off-white py-16 lg:py-24"`
   - This section sits between the dark hero and the feature showcase, providing visual rhythm

2. Inner container: `div.mx-auto.max-w-7xl.px-4.sm:px-6.lg:px-8`

3. Section heading (optional, use Claude's discretion -- could be "Why construction teams choose Fierro" or skip heading entirely and let cards speak for themselves)

4. Card grid with `data-animate-stagger` attribute:
   - Grid layout: `grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4`
   - If using 3 cards: `lg:grid-cols-3`

5. Each card has `data-animate` attribute and contains:
   - Icon area: use inline SVG icons (simple geometric -- not emoji). Examples: dollar sign in circle, bar chart, clock, shield
   - Title (h3): font-bold text-lg text-gunmetal
   - Description (p): text-sm text-concrete-gray mt-2
   - Card styling: rounded-lg border border-concrete-gray/20 bg-white p-6 text-center shadow-sm

6. Write 4 value prop cards with benefit-framed copy (Claude's discretion per CONTEXT.md). Suggested cards:
   - "Real-Time Cost Visibility" -- See every dollar across every project as it happens, not after the damage is done
   - "Zero Budget Surprises" -- Track costs against budgets before overruns become change orders
   - "Field + Office in One App" -- Capture expenses on the job site, review reports at your desk. Same data, no double entry
   - "Built for How You Work" -- Designed around construction workflows, not adapted from generic accounting software

Builder-voiced copy: direct, confident, construction vocabulary. NOT generic SaaS speak.

All icons should be simple inline SVGs (8-12 lines each) using currentColor or brand color values. Do NOT import external SVG files for these small icons.
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && test -f src/components/landing/ValueProps.astro && grep -c "data-animate" src/components/landing/ValueProps.astro && grep -c "data-animate-stagger" src/components/landing/ValueProps.astro && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>ValueProps.astro renders 3-4 benefit-framed cards in a responsive grid on off-white background. Each card has an inline SVG icon, bold title, and descriptive text. Cards use data-animate for scroll reveal with data-animate-stagger on parent for staggered entry. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds with no errors
- global.css contains animation infrastructure ([data-animate], stagger, reduced-motion)
- Hero.astro contains both CTAs with correct hrefs
- ValueProps.astro contains 3-4 cards with data-animate attributes
- All new files exist in src/components/landing/ directory
- SVG illustration exists in src/assets/illustrations/
</verification>

<success_criteria>
- Animation CSS system ready for all landing page sections (used by this plan and Plan 02)
- Hero section visually matches spec: dark Gunmetal bg, headline, subtitle, two CTAs, SVG illustration
- Value prop cards render in responsive grid with benefit-framed construction copy
- No new npm dependencies added
- Build succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-01-SUMMARY.md`
</output>
