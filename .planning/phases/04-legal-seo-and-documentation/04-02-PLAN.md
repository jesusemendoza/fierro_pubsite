---
phase: 04-legal-seo-and-documentation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/index.astro
  - src/pages/pricing.astro
  - src/pages/why-fierro.astro
  - public/og/home.png
  - public/og/pricing.png
  - public/og/why-fierro.png
  - public/og/privacy.png
  - public/og/terms.png
  - tests/legal.spec.ts
  - tests/seo-meta.spec.ts
  - tests/structured-data.spec.ts
  - tests/sitemap.spec.ts
autonomous: true
requirements:
  - PERF-04
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "Every page has a unique title and meta description visible in page source"
    - "Every page has Open Graph tags with a working og:image URL"
    - "Home page source contains Organization + SoftwareApplication JSON-LD with pricing tiers"
    - "OG images exist at /og/{page}.png and return 200 status"
    - "Playwright tests validate all SEO requirements and pass green"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Home page with SEO props and JSON-LD structured data"
      contains: "application/ld+json"
    - path: "src/pages/pricing.astro"
      provides: "Pricing page with unique description and OG image"
      contains: "description="
    - path: "src/pages/why-fierro.astro"
      provides: "Why Fierro page with unique description and OG image"
      contains: "description="
    - path: "public/og/home.png"
      provides: "OG social sharing image for home page"
    - path: "public/og/pricing.png"
      provides: "OG social sharing image for pricing page"
    - path: "public/og/why-fierro.png"
      provides: "OG social sharing image for why-fierro page"
    - path: "public/og/privacy.png"
      provides: "OG social sharing image for privacy page"
    - path: "public/og/terms.png"
      provides: "OG social sharing image for terms page"
    - path: "tests/legal.spec.ts"
      provides: "Playwright tests for LEGL-01, LEGL-02"
    - path: "tests/seo-meta.spec.ts"
      provides: "Playwright tests for PERF-04, PERF-05"
    - path: "tests/structured-data.spec.ts"
      provides: "Playwright tests for PERF-06"
    - path: "tests/sitemap.spec.ts"
      provides: "Playwright tests for PERF-07"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "description, ogImage props + JSON-LD in head slot"
      pattern: "slot=\"head\""
    - from: "src/pages/pricing.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "description, ogImage props"
      pattern: 'description="'
    - from: "tests/seo-meta.spec.ts"
      to: "all pages"
      via: "meta tag assertions"
      pattern: "og:title"
---

<objective>
Add page-specific SEO content (meta descriptions, OG images, JSON-LD structured data) across all pages, and create the full Playwright test suite for phase 4.

Purpose: With BaseLayout SEO infrastructure from Plan 01 in place, this plan populates every page with unique SEO metadata, creates branded OG images, adds JSON-LD structured data, and validates everything with automated tests.

Output: All 5 pages with unique SEO meta, 5 OG images in public/og/, JSON-LD on home page, 4 Playwright test files covering all phase 4 code requirements.
</objective>

<execution_context>
@/Users/jesus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jesus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-legal-seo-and-documentation/04-CONTEXT.md
@.planning/phases/04-legal-seo-and-documentation/04-RESEARCH.md
@.planning/phases/04-legal-seo-and-documentation/04-01-SUMMARY.md

<interfaces>
<!-- BaseLayout after Plan 01 extension -- Props interface -->
From src/layouts/BaseLayout.astro (after Plan 01):
```astro
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  ogType?: string;
  canonicalURL?: string;
}
```
Usage: `<BaseLayout title="Page Title" description="..." ogImage="/og/page.png">`
Head slot: `<script slot="head" type="application/ld+json" set:html={JSON.stringify(data)} />`

<!-- Current page imports (to be updated with SEO props) -->
From src/pages/index.astro:
```astro
<BaseLayout title="Construction Cost Control">
```

From src/pages/pricing.astro:
```astro
<BaseLayout title="Pricing">
```

From src/pages/why-fierro.astro:
```astro
<BaseLayout title="Why Fierro">
```

<!-- Existing Playwright pattern -->
From playwright.config.ts:
- baseURL: http://localhost:4321
- webServer: npm run dev on port 4321
- Projects: chromium only
- Test dir: ./tests/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OG images, page-specific SEO props, and JSON-LD structured data</name>
  <files>public/og/home.png, public/og/pricing.png, public/og/why-fierro.png, public/og/privacy.png, public/og/terms.png, src/pages/index.astro, src/pages/pricing.astro, src/pages/why-fierro.astro</files>
  <action>
1. **Create OG images** (1200x630 PNG) in public/og/ -- 5 images total.
   Use a programmatic approach: create an SVG template and convert to PNG, or use a Node.js script with sharp/canvas, or create minimal branded PNGs.

   Design per user decision: dark Gunmetal background (#2B3544), Fierro logo (text "FIERRO" in DM Sans Bold if no logo SVG available), page title in white DM Sans, tagline "Every dollar. Every pour. Accounted for." in off-white/60, Molten Orange accent element (horizontal line or bar).

   Files to create:
   - public/og/home.png -- title: "Construction Cost Control"
   - public/og/pricing.png -- title: "Simple, Transparent Pricing"
   - public/og/why-fierro.png -- title: "Why Fierro"
   - public/og/privacy.png -- title: "Privacy Policy"
   - public/og/terms.png -- title: "Terms of Service"

   Each image under 300KB. If programmatic generation is complex, create simple branded SVGs and convert to PNG using a build script, OR create them inline as base64-encoded minimal branded images. The key requirement is that they exist at the correct paths and are valid PNG files.

   **Practical approach:** Write a small Node.js script (scripts/generate-og-images.js) that uses the `canvas` npm package (or `@napi-rs/canvas`) to generate the 5 OG images. Install as a devDependency. Run it once to generate the PNGs, then commit the PNGs (not the script as a build step). If canvas packages have native dependency issues, fall back to creating simple solid-color PNG files using a pure-JS approach (e.g., writing raw PNG binary with pngjs or similar lightweight package).

2. **Add SEO props to index.astro:**
   - Add `description="Track construction budgets, expenses, and project health in real time. Free for solo contractors. No spreadsheets, no surprises."` to BaseLayout
   - Add `ogImage="/og/home.png"` to BaseLayout (this matches the default, but be explicit)
   - Add JSON-LD structured data using the head slot. Use `<script slot="head" type="application/ld+json" set:html={JSON.stringify(data)} />` pattern (CRITICAL: must use set:html to avoid Astro HTML escaping).
   - JSON-LD content: `@graph` array with Organization and SoftwareApplication types per RESEARCH.md Pattern 2. Include:
     - Organization: name "Fierro", url "https://getfierro.com", description, logo URL
     - SoftwareApplication: name "Fierro", applicationCategory "BusinessApplication", operatingSystem "Web", 3 pricing tier offers (Free $0, Plus $49/mo, Builder custom), featureList array
   - Do NOT change existing page content or component imports -- only add/modify the BaseLayout props and the JSON-LD script tag

3. **Add SEO props to pricing.astro:**
   - Add `description="Simple, transparent pricing for construction teams. Start free, upgrade to Plus at $49/mo when you're ready. No sales calls required."` to BaseLayout
   - Add `ogImage="/og/pricing.png"` to BaseLayout
   - Do NOT change existing page content

4. **Add SEO props to why-fierro.astro:**
   - Add `description="Spreadsheets lose money. Fierro gives contractors, subs, and project owners one place to track every dollar on every job."` to BaseLayout
   - Add `ogImage="/og/why-fierro.png"` to BaseLayout
   - Do NOT change existing page content

NOTE: privacy.astro and terms.astro already have SEO props from Plan 01. Do not modify them.
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && npm run build 2>&1 | tail -5 && echo "--- OG images ---" && ls -la public/og/*.png 2>/dev/null && echo "--- JSON-LD check ---" && grep -c "application/ld+json" dist/index.html 2>/dev/null && echo "--- Meta desc check ---" && grep -c 'meta name="description"' dist/index.html dist/pricing/index.html dist/why-fierro/index.html 2>/dev/null</automated>
  </verify>
  <done>All 5 pages have unique description and ogImage props. 5 OG PNG images exist in public/og/. index.astro contains JSON-LD with Organization + SoftwareApplication schema. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright test suite for phase 4 requirements</name>
  <files>tests/legal.spec.ts, tests/seo-meta.spec.ts, tests/structured-data.spec.ts, tests/sitemap.spec.ts</files>
  <action>
1. **Create tests/legal.spec.ts** (covers LEGL-01, LEGL-02):
   - Test /privacy page:
     - Page loads and has correct title containing "Privacy Policy"
     - Dark header band section exists (bg-gunmetal class)
     - h1 contains "Privacy Policy"
     - Effective date text is present ("March 2, 2026")
     - Prose content exists with multiple h2 section headings (at least 10)
     - prose-legal class is applied to the content article
     - Contact email "privacy@getfierro.com" appears on page
   - Test /terms page:
     - Page loads and has correct title containing "Terms of Service"
     - Dark header band section exists (bg-gunmetal class)
     - h1 contains "Terms of Service"
     - Effective date text is present ("March 2, 2026")
     - Prose content exists with multiple h2 section headings (at least 10)
     - prose-legal class is applied to the content article
     - Contact email "support@getfierro.com" appears on page

2. **Create tests/seo-meta.spec.ts** (covers PERF-04, PERF-05):
   - For EACH page (/, /pricing, /why-fierro, /privacy, /terms):
     - Has a unique `<title>` tag containing "| Fierro"
     - Has a `<meta name="description">` with non-empty content
     - Has `<meta property="og:title">` matching the page title
     - Has `<meta property="og:description">` with non-empty content
     - Has `<meta property="og:image">` with absolute URL starting with "https://getfierro.com/og/"
     - Has `<meta property="og:url">` with absolute URL
     - Has `<meta name="twitter:card">` with value "summary_large_image"
     - Has `<meta name="twitter:image">` with absolute URL
     - Has `<link rel="canonical">` with absolute URL
   - Verify all 5 pages have DIFFERENT title and description values (uniqueness check)

3. **Create tests/structured-data.spec.ts** (covers PERF-06):
   - Test home page (/) contains `<script type="application/ld+json">`
   - Parse the JSON-LD content and verify:
     - Contains @context "https://schema.org"
     - Contains @graph array
     - Has Organization type with name "Fierro" and url
     - Has SoftwareApplication type with name "Fierro"
     - SoftwareApplication has offers with 3 pricing tiers
     - SoftwareApplication has featureList array

4. **Create tests/sitemap.spec.ts** (covers PERF-07):
   - NOTE: Sitemap is generated at build time and the dev server may not serve it. Test approach:
     - Navigate to /sitemap-index.xml. If the dev server doesn't serve it, use a build-based assertion approach instead.
     - Alternative: Run `npm run build` before the test and check dist/sitemap-index.xml file exists and contains expected URLs.
     - Practical approach: Use Playwright to fetch http://localhost:4321/sitemap-index.xml. If the dev server serves static XML files, assert it contains the 5 page URLs. If not, skip the fetch test and add a build verification note.
   - Verify robots.txt is accessible at /robots.txt and contains "Sitemap:" directive

Follow existing test patterns from the project:
- Use `import { test, expect } from '@playwright/test';`
- Use `test.describe()` blocks for grouping
- Use `page.goto()` with paths relative to baseURL
- Use `page.locator()` for element assertions
- Use `page.content()` or `page.$eval()` for HTML content inspection
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && npx playwright test tests/legal.spec.ts tests/seo-meta.spec.ts tests/structured-data.spec.ts tests/sitemap.spec.ts --project=chromium -x 2>&1 | tail -30</automated>
  </verify>
  <done>All 4 test files exist and pass. Tests cover: legal page content/styling (LEGL-01, LEGL-02), unique meta tags per page (PERF-04), OG tags and images (PERF-05), JSON-LD structured data (PERF-06), sitemap generation (PERF-07). Full test suite (including prior phase tests) passes.</done>
</task>

</tasks>

<verification>
- `npx playwright test --project=chromium` passes (all tests including prior phases)
- Every page has unique `<title>` and `<meta name="description">` in build output
- `dist/index.html` contains valid JSON-LD with Organization + SoftwareApplication
- All 5 OG images exist in public/og/ and are valid PNGs
- Social sharing preview URLs (https://getfierro.com/og/*.png) resolve to images
</verification>

<success_criteria>
- All 5 pages have unique title, meta description, OG tags, and Twitter card tags
- 5 branded OG images (1200x630 PNG) exist in public/og/
- Home page contains JSON-LD structured data with Organization + SoftwareApplication + 3 pricing tiers
- 4 new Playwright test files pass covering LEGL-01, LEGL-02, PERF-04, PERF-05, PERF-06, PERF-07
- Full Playwright test suite (all phases) passes green
</success_criteria>

<output>
After completion, create `.planning/phases/04-legal-seo-and-documentation/04-02-SUMMARY.md`
</output>
