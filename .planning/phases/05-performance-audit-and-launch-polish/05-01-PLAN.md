---
phase: 05-performance-audit-and-launch-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Nav.astro
  - src/components/MobileMenu.astro
  - src/components/Footer.astro
  - src/components/landing/ValueProps.astro
  - src/components/pricing/PricingFaq.astro
  - public/_headers
autonomous: true
requirements: [PERF-08]

must_haves:
  truths:
    - "Pressing Tab on any page focuses a 'Skip to main content' link as the first focusable element"
    - "Activating the skip link moves focus past the sticky nav to the main content area"
    - "Mobile menu traps focus -- Tab/Shift+Tab cycles within menu items only"
    - "Pressing Escape closes the mobile menu and returns focus to the hamburger button"
    - "All inline SVG icons have aria-hidden='true' since surrounding text conveys meaning"
    - "Nav and footer landmark regions have distinct aria-label attributes"
    - "Hashed assets in /_astro/ return Cache-Control: public, max-age=31536000, immutable"
    - "HTML pages return Cache-Control: public, max-age=0, must-revalidate"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Skip-to-content link, main id and tabindex"
      contains: "skip-to-content"
    - path: "src/components/Nav.astro"
      provides: "Focus trap, Escape handler, aria-expanded, aria-label on nav"
      contains: "aria-expanded"
    - path: "src/components/MobileMenu.astro"
      provides: "aria-label on mobile nav landmark"
      contains: "aria-label"
    - path: "public/_headers"
      provides: "Cache-Control headers for hashed and unhashed assets"
      contains: "immutable"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "main#main-content"
      via: "skip link href=#main-content targets main with tabindex=-1"
      pattern: "href=\"#main-content\""
    - from: "src/components/Nav.astro"
      to: "src/components/MobileMenu.astro"
      via: "focus trap script queries #mobile-menu focusable elements"
      pattern: "focusable.*querySelectorAll"
---

<objective>
Add WCAG 2.1 AA accessibility patterns (skip-to-content link, mobile menu focus trapping, landmark aria-labels, decorative SVG marking) and cache headers to the Cloudflare _headers file.

Purpose: Fix known accessibility gaps and add performance-boosting cache headers before running the Lighthouse CI audit in Plan 02. These fixes address the PERF-08 requirement (responsive design + WCAG AA) and improve the Lighthouse accessibility score.
Output: Updated BaseLayout, Nav, MobileMenu, Footer, and _headers files with full WCAG 2.1 AA compliance and proper cache headers.
</objective>

<execution_context>
@/Users/jesus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jesus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-performance-audit-and-launch-polish/05-CONTEXT.md
@.planning/phases/05-performance-audit-and-launch-polish/05-RESEARCH.md

@src/layouts/BaseLayout.astro
@src/components/Nav.astro
@src/components/MobileMenu.astro
@src/components/Footer.astro
@src/components/landing/ValueProps.astro
@src/components/pricing/PricingFaq.astro
@src/styles/global.css
@public/_headers

<interfaces>
<!-- Key structures the executor needs -->

From src/layouts/BaseLayout.astro:
- Body contains: Nav, MobileMenu, <main>, Footer in that order
- <main> currently has NO id attribute
- ClientRouter is in <head>
- Named <slot name="head" /> for page-specific head content

From src/components/Nav.astro:
- <nav id="main-nav"> -- no aria-label currently
- #menu-toggle button has aria-label="Toggle menu" but no aria-expanded
- Script uses astro:page-load event with openMenu/closeMenu functions
- openMenu: removes hidden class, adds overflow-hidden to body
- closeMenu: adds hidden class, removes overflow-hidden from body
- No focus management, no Escape handler, no focus trap

From src/components/MobileMenu.astro:
- <div id="mobile-menu"> wraps a <nav> with no aria-label
- #menu-close button has aria-label="Close menu"
- Contains 5 links (Features, Pricing, Why Fierro, Login, Start Free)
- No focusable element management

From src/styles/global.css:
- [id] { scroll-margin-top: 5rem; } -- already handles skip link scroll offset
- @media (prefers-reduced-motion: reduce) already exists

From public/_headers:
- Only has security headers (X-Frame-Options, X-Content-Type-Options, etc.)
- No Cache-Control headers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skip-to-content link, landmark aria-labels, and decorative SVG marking</name>
  <files>
    src/layouts/BaseLayout.astro
    src/components/Nav.astro
    src/components/MobileMenu.astro
    src/components/Footer.astro
    src/components/landing/ValueProps.astro
    src/components/pricing/PricingFaq.astro
  </files>
  <action>
    1. In BaseLayout.astro, add a skip-to-content link as the FIRST child of body (before Nav):
       ```html
       <a
         href="#main-content"
         class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:rounded-md focus:bg-molten-orange focus:px-4 focus:py-2 focus:text-white focus:font-semibold focus:outline-none"
       >
         Skip to main content
       </a>
       ```

    2. In BaseLayout.astro, add `id="main-content"` and `tabindex="-1"` to the main element:
       ```html
       <main id="main-content" tabindex="-1">
       ```
       The existing `[id] { scroll-margin-top: 5rem; }` in global.css already handles the fixed nav offset.

    3. In Nav.astro, add `aria-label="Main navigation"` to the nav element:
       ```html
       <nav id="main-nav" aria-label="Main navigation" class="fixed ...">
       ```

    4. In MobileMenu.astro, add `aria-label="Mobile navigation"` to the inner nav element:
       ```html
       <nav class="flex flex-col ..." aria-label="Mobile navigation">
       ```

    5. In Footer.astro, the footer nav already has `aria-label="Footer navigation"` -- verify it is present (it is). Add `aria-label="Site footer"` to the outer footer element:
       ```html
       <footer aria-label="Site footer" class="border-t ...">
       ```

    6. Mark all decorative inline SVGs with `aria-hidden="true"`:
       - Nav.astro: The hamburger SVG icon inside #menu-toggle (the button already has aria-label, so the SVG is decorative)
       - MobileMenu.astro: The close SVG icon inside #menu-close (same pattern)
       - ValueProps.astro: All 4 card icon SVGs (the card text conveys the meaning)
       - PricingFaq.astro: The chevron/arrow SVG in the FAQ accordion (summary text conveys meaning)
       - The logo img in Nav.astro already has alt="Fierro" -- leave as-is (not decorative)

    Do NOT modify the Nav.astro script in this task -- focus trap is Task 2.
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && npm run build</automated>
  </verify>
  <done>
    - Skip-to-content link is first child of body in BaseLayout.astro
    - main element has id="main-content" and tabindex="-1"
    - Three nav/footer landmarks have distinct aria-label attributes
    - All decorative SVGs have aria-hidden="true"
    - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add mobile menu focus trap, Escape handler, and cache headers</name>
  <files>
    src/components/Nav.astro
    public/_headers
    .gitignore
  </files>
  <action>
    1. In Nav.astro, update the script block inside the `astro:page-load` listener. Replace the existing openMenu/closeMenu functions and add focus trap + Escape handler:

       openMenu function additions:
       - Set `toggle?.setAttribute('aria-expanded', 'true')` when opening
       - Call `menuClose?.focus()` to move focus into the menu

       closeMenu function additions:
       - Set `toggle?.setAttribute('aria-expanded', 'false')` when closing
       - Call `toggle?.focus()` to return focus to the hamburger button

       Add Escape key handler (outside menu but checking menu state):
       ```javascript
       document.addEventListener('keydown', (e) => {
         if (e.key === 'Escape' && !mobileMenu?.classList.contains('hidden')) {
           closeMenu();
         }
       });
       ```

       Add focus trap on the mobileMenu element:
       ```javascript
       mobileMenu?.addEventListener('keydown', (e) => {
         if (e.key !== 'Tab') return;
         const focusable = mobileMenu.querySelectorAll('a, button');
         const first = focusable[0];
         const last = focusable[focusable.length - 1];
         if (e.shiftKey && document.activeElement === first) {
           e.preventDefault();
           last.focus();
         } else if (!e.shiftKey && document.activeElement === last) {
           e.preventDefault();
           first.focus();
         }
       });
       ```

       Also add `aria-expanded="false"` as a default attribute on the #menu-toggle button in the HTML template (not just in JS).

    2. Update public/_headers to add cache headers BEFORE the existing security headers block. The final file should contain:

       ```
       # Hashed assets (CSS, JS, fonts in /_astro/) -- immutable, cache 1 year
       /_astro/*
         Cache-Control: public, max-age=31536000, immutable

       # HTML pages -- always revalidate for instant deploys
       /*.html
         Cache-Control: public, max-age=0, must-revalidate

       /*/index.html
         Cache-Control: public, max-age=0, must-revalidate

       # OG images -- cache 1 week (unhashed but rarely change)
       /og/*
         Cache-Control: public, max-age=604800

       # Favicon -- cache 1 month
       /favicon.ico
         Cache-Control: public, max-age=2592000

       # Security headers (all responses)
       /*
         X-Frame-Options: DENY
         X-Content-Type-Options: nosniff
         Referrer-Policy: strict-origin-when-cross-origin
         Permissions-Policy: camera=(), microphone=(), geolocation=()
       ```

    3. Add `.lighthouseci/` to .gitignore (prepares for Plan 02 which will install @lhci/cli):
       Add under a new `# lighthouse ci` comment section at the end of .gitignore.
  </action>
  <verify>
    <automated>cd /Users/jesus/sundownspecial/fierro/fierro_pubsite && npm run build</automated>
  </verify>
  <done>
    - Mobile menu opens with focus on close button, closes with focus back on hamburger
    - aria-expanded toggles between true/false on #menu-toggle
    - Escape key closes open mobile menu
    - Tab cycles within mobile menu when open (focus trap)
    - _headers file contains Cache-Control rules for /_astro/*, /*.html, /og/*, /favicon.ico
    - .gitignore includes .lighthouseci/
    - Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Built HTML contains skip-to-content link as first focusable element in body
- All nav landmarks have distinct aria-label values
- _headers file has both cache and security headers
- .gitignore includes .lighthouseci/
</verification>

<success_criteria>
- BaseLayout has skip-to-content link and main#main-content with tabindex="-1"
- Nav script manages focus (trap + Escape + aria-expanded)
- Three landmark regions (main nav, mobile nav, footer) have distinct aria-labels
- All decorative SVGs have aria-hidden="true"
- _headers has cache rules for hashed assets (1 year), HTML (revalidate), OG images (1 week), favicon (1 month)
- .gitignore includes .lighthouseci/
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-audit-and-launch-polish/05-01-SUMMARY.md`
</output>
